# Logs e arquivos de erro (ninguém precisa ver isso)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# A PASTA MAIS IMPORTANTE DE BLOQUEAR (Dependências gigantes)
node_modules

# Arquivos de build (o site pronto gerado pelo comando build)
dist
dist-ssr
*.local

# Arquivos de sistema operacional (lixo do Mac e Windows)
.DS_Store
Thumbs.db

# Arquivos de configuração do editor (VS Code, etc)
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variáveis de ambiente (pra não vazar senhas se um dia usar)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local